
{% extends "base.html.twig" %}

{% block title %}{% if user.id %}Edytuj {{ user.username }}{% else %}Dodaj nowego użytkownika{% endif %} - {{ setting('title') }}{% endblock %}

{% block content %}
<div class="page-header">
	<h1><i class="fas fa-user"></i> {% if user.id %}Edytuj {{ user.username }}{% else %}Dodaj nowego użytkownika{% endif %}</h1>
	<p><a href="{{ path('user_list') }}">Wróć do listy użytkowników</a></p>
</div>

{% include 'partial/alert.html.twig' %}

{{ form(form) }}

{% endblock %}
